ktor:
    application:
        modules:
            - cloud.karpov.ApplicationKt.module
    deployment:
        port: 8080
database:
  driver: org.postgresql.Driver 
  url: jdbc:postgresql://localhost:5432/maindb 
  user: test
  password: test
jwt:
  domain: "https://jwt-provider-domain/"
  audience: "jwt-audience"
  realm: "ktor sample app"
